import { db } from './firebase-init.js?v=20260907-1';
import { getAuthManager } from './auth.js?v=20260907-1';
import { collection, getDocs, getDoc, doc, setDoc, updateDoc, query, orderBy, serverTimestamp, increment } from 'https://www.gstatic.com/firebasejs/10.7.0/firebase-firestore.js';

const root=document.getElementById('affiliate-admin-app');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
class AffiliateAdmin {
  constructor(){this.auth=getAuthManager();this.user=null;this.applications=[];this.accounts=[];this.rates=[];this.commissions=[];this.payouts=[];}
  async init(){await this.auth.waitForAuthReady?.();this.user=this.auth.getCurrentUser?.();if(!this.user){this.login();return;}const profile=await getDoc(doc(db,'clients',this.user.uid));if(!profile.exists()||String(profile.data()?.role||'').toLowerCase()!=='admin'){this.denied();return;}await this.load();this.render();this.bind();}
  async load(){const read=async(name,sort='updatedAt')=>{try{return (await getDocs(query(collection(db,name),orderBy(sort,'desc')))).docs.map(s=>({id:s.id,...s.data()}));}catch{return (await getDocs(collection(db,name))).docs.map(s=>({id:s.id,...s.data()}));}};[this.applications,this.accounts,this.rates,this.commissions,this.payouts]=await Promise.all([read('affiliateApplications'),read('affiliateAccounts'),read('affiliateDepartmentRates','departmentId'),read('affiliateCommissions'),read('affiliatePayouts')]);this.payouts=this.payouts.filter(p=>p.affiliateId&&p.status!=='LEGACY');}
  login(){root.innerHTML='<main class="aff-public-main"><section class="aff-panel" style="max-width:560px;margin:auto"><p class="aff-eyebrow">Smart Cut Admin</p><h1>Accès administrateur</h1><p>Connectez-vous pour gérer le programme d’affiliation.</p><button class="aff-btn aff-btn--gold" data-login>Se connecter</button></section></main>';root.querySelector('[data-login]').onclick=()=>this.auth.openAuthModal?.('login');document.addEventListener('authChanged',()=>this.init(),{once:true});}
  denied(){root.innerHTML='<main class="aff-public-main"><section class="aff-panel" style="max-width:560px;margin:auto"><p class="aff-eyebrow">Accès refusé</p><h1>Compte administrateur requis.</h1><a class="aff-btn aff-btn--outline" href="./affiliate.html">Retour au programme</a></section></main>';}
  render(){root.innerHTML=`<header class="aff-topbar"><a class="aff-brand" href="./affiliate.html"><span class="aff-brand-mark"><i class="fas fa-share-nodes"></i></span><span>Smart Cut<small>Affiliate admin</small></span></a><button class="aff-btn aff-btn--gold" data-logout>Déconnexion</button></header><div class="aff-layout"><aside class="aff-aside"><div class="aff-aside-title">Pilotage</div><nav class="aff-nav"><button class="is-active" data-admin-target="admin-overview">Vue d’ensemble</button><button data-admin-target="admin-applications">Demandes</button><button data-admin-target="admin-rates">Taux par département</button><button data-admin-target="admin-commissions">Commissions</button></nav></aside><main class="aff-content"><div id="admin-overview" class="aff-content-head"><div><p class="aff-eyebrow">Programme d’affiliation</p><h1>Contrôle centralisé.</h1><p>Validez les affiliés et pilotez les taux client/commission.</p></div><span class="aff-status">ADMIN</span></div><section class="aff-kpis"><div class="aff-kpi"><span>Demandes en attente</span><strong>${this.applications.filter(a=>a.status==='PENDING').length}</strong></div><div class="aff-kpi"><span>Affiliés actifs</span><strong>${this.accounts.filter(a=>a.status==='ACTIVE').length}</strong></div><div class="aff-kpi"><span>Commissions en attente</span><strong>${this.commissions.filter(c=>c.status==='PENDING').length}</strong></div><div class="aff-kpi"><span>Paiements à traiter</span><strong>${this.payouts.filter(p=>p.status==='REQUESTED').length}</strong></div></section><section id="admin-applications" class="aff-panel"><div class="aff-panel-head"><h2>Demandes affiliés</h2><span class="aff-status">${this.applications.length} total</span></div><div class="aff-table-wrap"><table class="aff-table"><thead><tr><th>Profil</th><th>Canal</th><th>Statut</th><th>Action</th></tr></thead><tbody>${this.applications.length?this.applications.map(a=>`<tr><td><strong>${esc(a.fullName||a.name||a.userId)}</strong><small>${esc(a.email||a.phone||'')}</small></td><td>${esc(a.promotionType||'—')}</td><td><span class="aff-status">${esc(a.status||'PENDING')}</span></td><td>${a.status==='PENDING'?`<button class="aff-btn aff-btn--gold aff-btn--small" data-approve="${esc(a.id)}">Approuver</button> <button class="aff-btn aff-btn--outline aff-btn--small" data-reject="${esc(a.id)}">Refuser</button>`:'—'}</td></tr>`).join(''):'<tr><td colspan="4"><div class="aff-empty">Aucune demande.</div></td></tr>'}</tbody></table></div></section><section class="aff-panel"><div class="aff-panel-head"><h2>Paiements affiliés</h2><span class="aff-status">${this.payouts.length} demande(s)</span></div><div class="aff-table-wrap"><table class="aff-table"><thead><tr><th>Affilié</th><th>Montant</th><th>Destination</th><th>Statut</th><th></th></tr></thead><tbody>${this.payouts.length?this.payouts.map(p=>`<tr><td>${esc(p.affiliateId)}</td><td><strong>${Number(p.amount||0).toLocaleString('fr-FR')} HTG</strong></td><td>${esc(p.destination||'—')}</td><td><span class="aff-status">${esc(p.status||'REQUESTED')}</span></td><td>${p.status==='REQUESTED'?`<button class="aff-btn aff-btn--gold aff-btn--small" data-paid="${esc(p.id)}">Marquer payé</button>`:'—'}</td></tr>`).join(''):'<tr><td colspan="5"><div class="aff-empty">Aucune demande de paiement.</div></td></tr>'}</tbody></table></div></section><section id="admin-commissions" class="aff-panel"><div class="aff-panel-head"><h2>Commissions à valider</h2><span class="aff-status">${this.commissions.length} total</span></div><div class="aff-table-wrap"><table class="aff-table"><thead><tr><th>Affilié</th><th>Produit</th><th>Montant</th><th>Statut</th><th></th></tr></thead><tbody>${this.commissions.length?this.commissions.slice(0,40).map(c=>`<tr><td>${esc(c.affiliateId)}</td><td>${esc(c.productName||c.productId)}</td><td><strong>${Number(c.commissionAmount||0).toLocaleString('fr-FR')} HTG</strong></td><td><span class="aff-status">${esc(c.status||'PENDING')}</span></td><td>${c.status==='PENDING'?`<button class="aff-btn aff-btn--gold aff-btn--small" data-available="${esc(c.id)}">Valider</button>`:'—'}</td></tr>`).join(''):'<tr><td colspan="5"><div class="aff-empty">Aucune commission.</div></td></tr>'}</tbody></table></div></section><section id="admin-rates" class="aff-panel"><div class="aff-panel-head"><h2>Taux par département</h2><span class="aff-status">Total recommandé ≤ 100 %</span></div><div class="aff-table-wrap"><table class="aff-table"><thead><tr><th>Département</th><th>Commission affilié (%)</th><th>Réduction client (%)</th><th>État</th><th></th></tr></thead><tbody>${this.rates.length?this.rates.map(r=>`<tr><td><strong>${esc(r.label||r.departmentId)}</strong><small>${esc(r.departmentId)}</small></td><td><input class="aff-input" type="number" min="0" max="100" step="0.1" data-rate="${esc(r.id)}" value="${Number(r.affiliateCommissionRate??r.affiliateRate??r.rate??0)}"></td><td><input class="aff-input" type="number" min="0" max="100" step="0.1" data-discount="${esc(r.id)}" value="${Number(r.customerDiscountRate||0)}"></td><td><span class="aff-status">${r.active===false?'INACTIF':'ACTIF'}</span></td><td><button class="aff-btn aff-btn--gold aff-btn--small" data-save-rate="${esc(r.id)}">Enregistrer</button></td></tr>`).join(''):'<tr><td colspan="5"><div class="aff-empty">Aucun département configuré.</div></td></tr>'}</tbody></table></div></section></main></div>`;}
  bind(){root.querySelector('[data-logout]')?.addEventListener('click',()=>this.auth.logout?.());root.querySelectorAll('[data-admin-target]').forEach(b=>b.onclick=()=>document.getElementById(b.dataset.adminTarget)?.scrollIntoView({behavior:'smooth',block:'start'}));root.querySelectorAll('[data-approve]').forEach(b=>b.onclick=()=>this.review(b.dataset.approve,'APPROVED'));root.querySelectorAll('[data-reject]').forEach(b=>b.onclick=()=>this.review(b.dataset.reject,'REJECTED'));root.querySelectorAll('[data-save-rate]').forEach(b=>b.onclick=()=>this.saveRate(b.dataset.saveRate));root.querySelectorAll('[data-paid]').forEach(b=>b.onclick=()=>this.markPaid(b.dataset.paid));root.querySelectorAll('[data-available]').forEach(b=>b.onclick=()=>this.setCommissionStatus(b.dataset.available,'AVAILABLE'));}
  async review(id,status){const item=this.applications.find(a=>a.id===id);if(!item)return;await updateDoc(doc(db,'affiliateApplications',id),{status,reviewedAt:serverTimestamp(),reviewedBy:this.user.uid});if(status==='APPROVED'){await setDoc(doc(db,'affiliateAccounts',item.userId),{userId:item.userId,status:'ACTIVE',displayName:item.fullName||item.name||'',approvedAt:serverTimestamp(),approvedBy:this.user.uid},{merge:true});}await this.load();this.render();this.bind();}
  async saveRate(id){const rate=Number(root.querySelector(`[data-rate="${CSS.escape(id)}"]`)?.value||0);const discount=Number(root.querySelector(`[data-discount="${CSS.escape(id)}"]`)?.value||0);if(rate<0||discount<0||rate+discount>100){alert('Le total des taux doit rester inférieur ou égal à 100 %.');return;}await updateDoc(doc(db,'affiliateDepartmentRates',id),{affiliateCommissionRate:rate,customerDiscountRate:discount,updatedAt:serverTimestamp(),updatedBy:this.user.uid});const row=this.rates.find(r=>r.id===id);if(row){row.affiliateCommissionRate=rate;row.customerDiscountRate=discount;}this.render();this.bind();}
  async markPaid(id){const payout=this.payouts.find(p=>p.id===id);if(!payout)return;await updateDoc(doc(db,'affiliatePayouts',id),{status:'PAID',paidAt:serverTimestamp(),updatedAt:serverTimestamp(),processedBy:this.user.uid});for(const commissionId of payout.commissionIds||[]) await updateDoc(doc(db,'affiliateCommissions',commissionId),{status:'PAID',paidAt:serverTimestamp(),updatedAt:serverTimestamp()});await updateDoc(doc(db,'affiliateAccounts',payout.affiliateId),{availableCommission:increment(-Number(payout.amount||0)),paidCommission:increment(Number(payout.amount||0)),updatedAt:serverTimestamp()});await this.load();this.render();this.bind();}
  async setCommissionStatus(id,status){const commission=this.commissions.find(item=>item.id===id);if(!commission)return;const amount=Number(commission.commissionAmount||0);await updateDoc(doc(db,'affiliateCommissions',id),{status,updatedAt:serverTimestamp(),reviewedBy:this.user.uid});if(commission.status==='PENDING'&&status==='AVAILABLE') await updateDoc(doc(db,'affiliateAccounts',commission.affiliateId),{pendingCommission:increment(-amount),availableCommission:increment(amount),updatedAt:serverTimestamp()});await this.load();this.render();this.bind();}
}
new AffiliateAdmin().init().catch(e=>{console.error(e);root.innerHTML='<main class="aff-public-main"><section class="aff-panel"><h1>Chargement impossible</h1><p>Réessayez après vous être reconnecté.</p></section></main>';});
